# راهنمای دموی بصری AgentChat

## اجرای ۳۰ ثانیه‌ای

```powershell
# یک دستور — Chrome CDP به‌صورت خودکار اجرا می‌شود
node scripts/demo_server.js
```

سپس مرورگر را روی **http://localhost:3456** باز کنید تا پلتفرم کامل Demo نمایش داده شود.

---

## نمای کلی پلتفرم Demo

```text
http://localhost:3456/
├── 🏠 صفحه اصلی          — کارت‌های Skill + وضعیت لحظه‌ای CDP
├── 📐 معماری             — پشته ۴ لایه + نمودار جریان داده + خط زمانی مشارکت
├── 🔗 زنجیره Fallback    — انتخاب Provider → ورود سؤال → دریافت پاسخ
├── ⚡ ارکستراسیون موازی  — قالب آماده یا موضوع سفارشی → تحلیل موازی
├── 🔄 پایپ‌لاین ترتیبی   — موضوع → Kimi → Gemini → ChatGPT
├── 🔌 MCP Server         — نمایش ابزارها + راهنمای تنظیم Claude Desktop
├── 🐍 Python SDK         — جدول API + مثال‌های کدنویسی
└── 🌐 چندزبانه           — جدول مقایسه زبان‌ها + جزئیات فنی
```

---

## قابلیت‌های تعاملی صفحات

| صفحه | قابلیت | API |
|---|---|---|
| **زنجیره Fallback** | انتخاب Provider → سؤال → ارسال → مشاهده پاسخ و زنجیره جایگزینی | `POST /api/ask` |
| **ارکستراسیون موازی** | انتخاب قالب و موضوع → اجرای تحلیل توسط چند AI | `POST /api/ask` ×4 |
| **پایپ‌لاین ترتیبی** | موضوع → جست‌وجوی Kimi → استدلال Gemini → بررسی ChatGPT | `POST /api/ask` ×3 |
| **MCP Server** | نمایش لحظه‌ای وضعیت اتصال CDP | `GET /api/health` |

## APIهای موجود

| روش | مسیر | کاربرد |
|---|---|---|
| `POST` | `/api/ask` | پرسش از یک Provider: `{prompt, provider}` |
| `POST` | `/api/parallel` | ارکستراسیون موازی Workerها |
| `POST` | `/api/search-web` | جست‌وجوی وب با Kimi |
| `POST` | `/api/deep-reason` | استدلال عمیق Gemini |
| `POST` | `/api/review` | بررسی متقابل با ChatGPT |
| `POST` | `/api/verify` | بررسی واقعیت با Qwen |
| `GET` | `/api/smoke` | بررسی دسترسی Providerها |
| `GET` | `/api/health` | بررسی سلامت CDP |
| `GET` | `/api/stats` | آمار سرور |

## نیازمندی‌ها

| مورد | حداقل نیاز |
|---|---|
| Node.js | 18 یا بالاتر |
| Chrome | آخرین نسخه پایدار |
| فضای دیسک | 200MB |
| شبکه | دسترسی به `gemini.google.com` |
| حساب Google | اشتراک Gemini Pro توصیه می‌شود |

## نکات ارائه برای مصاحبه

1. **شروع:** این پروژه با Chrome CDP هشت سرویس AI را کنترل می‌کند و می‌تواند هر Provider را به‌صورت زنده فراخوانی کند.
2. **زنجیره Fallback:** یک سؤال ارسال کنید و جابه‌جایی خودکار بین Providerها را نمایش دهید.
3. **ارکستراسیون موازی:** یک قالب آماده را اجرا کنید تا چند Worker هم‌زمان ابعاد مختلف مسئله را تحلیل کنند.
4. **پایپ‌لاین ترتیبی:** یک موضوع پژوهشی وارد کنید و مسیر جست‌وجو → استدلال → بررسی را نمایش دهید.
5. **جمع‌بندی:** صفحه Python SDK را باز کنید و نمونه استفاده از `from agentchat import GeminiSession` را نشان دهید.
